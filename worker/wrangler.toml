name = "commit-jugaadism"
main = "src/index.js"
compatibility_date = "2024-12-01"
workers_dev = true
preview_urls = true

# Optional: change model without redeploying secrets (see https://openrouter.ai/models)
[vars]
OPENROUTER_MODEL = "stepfun/step-3.5-flash:free"
